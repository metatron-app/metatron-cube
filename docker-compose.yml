# Local single-node Druid (metatron-cube) stack:
#   zookeeper + postgres (metadata) + coordinator/overlord/broker/historical/middleManager
#
#   docker compose up -d --build          # build image + start everything
#   docker compose logs -f broker         # follow a role
#   docker compose down                   # stop (add -v to wipe data volumes)
#
# Config lives in ./docker/conf/druid (bind-mounted read-only onto every role).
# Edit ./docker/conf/druid/_common/common.runtime.properties to retarget DB etc.
name: metatron

x-druid-common: &druid-common
  image: metatron-cube:2021.3
  build:
    context: .
  restart: unless-stopped
  volumes:
    - ./docker/conf/druid:/opt/druid/conf/druid:ro
    - ./docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh:ro
    - druid_var:/opt/druid/var
  depends_on:
    postgres:
      condition: service_healthy
    zookeeper:
      condition: service_healthy
  networks: [druid]

services:
  zookeeper:
    image: zookeeper:3.9
    environment:
      ZOO_4LW_COMMANDS_WHITELIST: "ruok,stat,conf,mntr"
    healthcheck:
      test: ["CMD-SHELL", "echo ruok | nc -w 2 localhost 2181 | grep -q imok"]
      interval: 10s
      timeout: 5s
      retries: 12
    networks: [druid]

  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: druid
      POSTGRES_USER: druid
      POSTGRES_PASSWORD: diurd
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U druid -d druid"]
      interval: 10s
      timeout: 5s
      retries: 12
    networks: [druid]

  # coordinator runs with druid.coordinator.asOverlord.enabled=true, so it also
  # serves the overlord API at :8081/druid/indexer/* (no separate overlord process).
  coordinator:
    <<: *druid-common
    command: ["coordinator"]
    ports:
      - "8081:8081"

  broker:
    <<: *druid-common
    command: ["broker"]
    ports:
      - "8082:8082"

  historical:
    <<: *druid-common
    command: ["historical"]
    ports:
      - "8083:8083"

  # No middleManager: the embedded overlord uses the forking (local) task runner,
  # so it forks peons in the coordinator process. Add a middleManager and set
  # druid.indexer.runner.type=remote (coordinator config) to distribute tasks instead.

volumes:
  druid_var:
  pg_data:

networks:
  druid:
