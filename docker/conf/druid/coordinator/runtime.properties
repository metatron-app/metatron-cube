druid.service=druid/coordinator
druid.port=8081

druid.coordinator.startDelay=PT30S
druid.coordinator.period=PT30S

# Run the overlord embedded in this process (single coordinator+overlord node).
# The overlord API is served locally at :8081/druid/indexer/*, and this node also
# announces itself under druid/overlord for discovery.
druid.coordinator.asOverlord.enabled=true
druid.indexer.queue.startDelay=PT30S
druid.indexer.runner.type=remote
druid.indexer.storage.type=metadata
